# 04_improve — correction becomes learning

Fine-tune on recorded corrections and measure whether the intervention rate falls.

**Proves:** the whole project.
**Needs:** `[sim,train]` and one consumer GPU.

**Check:** the graph. Cumulative corrections on x, intervention rate on y. If the line is
flat, the loop does not close, and that result belongs in the README as prominently as a
success would.
